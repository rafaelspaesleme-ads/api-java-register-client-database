       server {
               listen        80;
               server_name   SUB_DOMAIN.DOMAIN;

               location / {
                   proxy_pass      http://172.0.0.1;
               }
       }